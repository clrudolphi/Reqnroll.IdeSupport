{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Gradle: runIde",
      "type": "shell",
      "command": "./gradlew",
      "windows": {
        "command": ".\\gradlew.bat"
      },
      "args": ["runIde"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [],
      "detail": "Republishes the LSP server (only if src/LSP or src/Core changed — see publishServer's Gradle inputs in build.gradle.kts) and launches a sandboxed Rider instance with the plugin loaded."
    }
  ]
}
